const assert = require('assert')

describe('pinningList', function () {
  let pinningList

  before(async () => {
    pinningList = require('../lib/pinningList')
  })

  it('does something...', async () => {
    assert(true)
  })

  after(async () => {
  })
})
